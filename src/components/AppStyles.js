import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  container: {
    flex: 1, backgroundColor: '#E7E7E2', width: '100%'
  },
  buttonStyle: {
    borderRadius: 0, marginLeft: 0, marginRight: 0, marginBottom: 0
  }
});

export default globalStyles;
